# Installation

1. `git clone --recurse-submodules https://github.com/Farmerobot/among_them_thesis_context.git`
2. Download the LaTeX thesis project as a zip file from overleaf
3. Unzip the into latex_project folder